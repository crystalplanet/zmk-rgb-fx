// So, how are we going to do this?
// 
// Need a similar indicator to WPM - could eventually maybe even turn this into a single 'gauge' animation,
// and just pass it different sensors.
//
// We want two things:
// - Battery charge state
// - Charging / Full / Empty indicators
//
// What if we also had another 'general' indicator animation, that could do all kinds of things.
// You need to select the TYPE and supply it with an underlying animation and that's
